<% if @feed_items.any? %>
  <ol class="microposts">
    <% for item in @feed_items %>
      <%= render item %>
    <% end %>
  </ol>
  <%= will_paginate @feed_items %>
<% else %>
  Your feed is empty! Maybe you should follow someone.
<% end %>
